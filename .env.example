# football-data.org API token (free tier)
FOOTBALL_DATA_TOKEN=
# CockroachDB connection
CRDB_HOST=
CRDB_USER=
CRDB_PASSWORD=
CRDB_DATABASE=worldcup
# (CRDB_PORT=26257 and CRDB_SSLMODE=require have defaults in code)